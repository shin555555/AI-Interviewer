# 開発進捗・ロードマップ (開発ストーリー)

このファイルは、単なるTODOリスト（`status.md`）の枠を超え、プロジェクトの過去の経緯、現在の立ち位置、そして未来のロードマップ（次に何をすべきか、なぜそれが必要か）を記述するためのものです。
AIエージェントはこのファイルを読み込むことで、「今どのフェーズにいて、今後どういった拡張やテストが控えているのか」という大局的なコンテキストを理解し、「すべてのタスクが終わったからやることがない」と誤認することを防ぎます。

## これまでの歩み（完了したフェーズ）
- **フェーズ1: フロントエンド基盤構築**: React/Viteによるプロジェクト生成、CSS変数の定義、ルーティング設計。
- **フェーズ2: インタビューエンジン**: 30問の質問UI、ステップ形式、文字サイズ変更、セッション保持機能の実装。
- **フェーズ3: 解析・アルゴリズム**: スコア計算、MatchScoreロジック（柔軟性の反転ロジック含む）の実装。
- **フェーズ4: 職種データベース**: 40職種の静的マスターデータ作成。
- **フェーズ5: 結果表示・PDF出力**: Chart.jsによるレーダーチャート、やさしい日本語による解説出力、PDF化機能。
- **フェーズ6: 管理者ダッシュボードUI**: Chart.jsによる全体統計表示（ドーナツ、棒グラフ等）、個別ユーザー時系列データのプロットUI。
- **フェーズ7: GASバックエンド連携**: Google Apps Scriptを利用し、フロントエンドとスプレッドシート間でAPI通信する基盤。`Code.gs`の作成、フロントエンド(`spreadsheetAPI.js`, `App.jsx`, `ResultScreen.jsx`)でのAPI連携・POST送信の実装が完了した。
- **フェーズ8: UX/UIのブラッシュアップと微調整 (現在進行中)**: 例外エラー処理、スマホ実機でのUI調整、スプレッドシートマスター側の動作確認を行う。

GAS連携によるE2Eテストが無事に完了し、フロントからスプレッドシートへのデータ疎通が確認できました。
現在、実運用を見据えたUI/UXの最終的なブラッシュアップ（エラー時のリカバリー表示や、モバイルでのレスポンシブな操作感の微調整）を行うフェーズに入っています。

## 次のステップ（未着手・今後のロードマップ）
チェックリスト（`status.md`）が枯渇していても、以下のような中長期的なタスクが存在します。AIはこれを常に意識し、自律的に提案を行ってください。

*   **スプレッドシート上でのグラフ自動描画**: GAS側を拡張し、受信したデータに基づいてスプレッドシートのシート内にもグラフ（円グラフ等）を自動生成する機能の追加。
*   **スプレッドシート上でのグラフ自動描画**: GAS側を拡張し、受信したデータに基づいてスプレッドシートのシート内にもグラフ（円グラフ等）を自動生成する機能の追加。
*   **例外処理とUXの向上**: GAS側でエラーが起きたり、通信が不安定な際、ユーザーが迷わず再試行できるリカバリーUIの追加。
*   **デザイン・機能の修正**:
    *   不要な「成長記録（時系列比較）」グラフの削除。
    *   不要な「要注意アラート」表示の削除。
*   **デザインの微調整**: グラフのアニメーションや、スマートフォンの実機での細かい操作感の修正。
*   **第2フェーズでの追加修正**:
    *   自由記述欄入力時に3択の強制を解除し、「次へ進む」ボタンを追加。
    *   3択の「倒れるまで気づかない」を「気づかない」へ文言変更。
    *   スプレッドシートへのデータ重複保存（2行保存）バグの修正。
    *   回答時間がまた異常になるバグの再検証・修正。
    *   結果画面からトップページ（StartScreen）へ戻る「終わる」ボタンの追加。
*   **職種マスターの調整**: スプレッドシート上の「職種マスター」シートを編集してみて、マッチング結果が意図通りに変わるかのテスト。
*   **セキュリティ強化**: デモから実践投入へ向けて、管理画面の認証や個人情報保護の厳密化。
*   **フェーズ9: 大規模アップデート（マッチング精度とバリエーションの向上）**:
    *   **質問プールの拡充（全90問化）**: 現在の固定30問から、各属性9問×10属性＝計90問へバリエーションを増やす。
    *   **ランダム出題システム**: 毎回90問の中から、各属性3問ずつ（計30問）をランダムに抽出して出題し、複数回テストを受ける際の「慣れ」を防止する。
    *   **選択肢の拡充（解像度のアップ）**: 3択から具体的な4〜5択に変更。同点を減らし、人物の「色の濃淡」をより正確にスコアリングすることで、40種類の職種とのマッチング精度を飛躍的に高める。
*   **フェーズ10: 本番公開と多拠点展開の準備**:
    *   **自事業所内でのホスティング**: GitHub Pages等を利用。コマンドなしで、ブックマークから誰でも起動できるようにする。
    *   **多拠点対応（マルチテナント設計）**: 将来的に他事業所に提供する際、データをごちゃ混ぜにしないための仕組み作り。
        *   **案1（マルチテナント型）**: ログイン画面で事業所を識別し、単一DB内でデータを分離。
        *   **案2（エンドポイント分離型）**: 事業所ごとに保存先スプレッドシートを分け、アプリ側で接続先を設定。
*   **フェーズ11: 経験ベースのマッチング強化と職種データベースの拡充（大規模アップデート第2弾）**:
    *   **背景と仮説**: 「性格（ポテンシャル）」でマッチ度を測る従来の方式に加え、「これまでの経験・趣味・日常のちょっとした行動」を掛け合わせることで、さらに精度の高い、具体的な「できること」に直結するマッチング結果が出せるのではないかという仮説。
    *   **2部構成へ進化**: 性格テスト（30問）終了後、新たに「経験・スキルチェック（10問）」を行う第2部を構築。
    *   **経験質問プールの開発**: 「仕事の経験」だけでなく「趣味や日常（ゲーム、細かい家事、SNS発信等）」に根ざした強みを発掘するため、カテゴリー別に30〜50問の広範な経験質問プールを作り、毎回ランダムに10問を抽出。
    *   **ユニークな受託業務への対応（60〜80職種への拡充）**: IT系（アノテーション、動画編集、QA等）、クリエイティブ系（レタッチ、ライティング）、専門系（屋内農業等）などを大幅に追加し、近年のA型・B型の多様な実践業務とリンクさせる。

