# 開発進捗・ロードマップ (開発ストーリー)

このファイルは、単なるTODOリスト（`status.md`）の枠を超え、プロジェクトの過去の経緯、現在の立ち位置、そして未来のロードマップ（次に何をすべきか、なぜそれが必要か）を記述するためのものです。
AIエージェントはこのファイルを読み込むことで、「今どのフェーズにいて、今後どういった拡張やテストが控えているのか」という大局的なコンテキストを理解し、「すべてのタスクが終わったからやることがない」と誤認することを防ぎます。

## これまでの歩み（完了したフェーズ）
- **フェーズ1: フロントエンド基盤構築**: React/Viteによるプロジェクト生成、CSS変数の定義、ルーティング設計。
- **フェーズ2: インタビューエンジン**: 30問の質問UI、ステップ形式、文字サイズ変更、セッション保持機能の実装。
- **フェーズ3: 解析・アルゴリズム**: スコア計算、MatchScoreロジック（柔軟性の反転ロジック含む）の実装。
- **フェーズ4: 職種データベース**: 40職種の静的マスターデータ作成。
- **フェーズ5: 結果表示・PDF出力**: Chart.jsによるレーダーチャート、やさしい日本語による解説出力、PDF化機能。
- **フェーズ6: 管理者ダッシュボードUI**: Chart.jsによる全体統計表示（ドーナツ、棒グラフ等）、個別ユーザー時系列データのプロットUI。
- **フェーズ7: GASバックエンド連携 (現在進行中)**: Google Apps Scriptを利用し、フロントエンドとスプレッドシート間でAPI通信する基盤。`Code.gs`の作成、フロントエンド(`spreadsheetAPI.js`, `App.jsx`, `ResultScreen.jsx`)でのAPI連携・POST送信の実装が完了した。

## 現在の立ち位置（今やっていること）
GASバックエンドとフロントエンドを連携するコード上の実装は一通り完了しました。
しかし、これは**ローカルでの実装が完了しただけ**であり、実際にGoogleスプレッドシート上でエンドツーエンド(E2E)でデータが正しく読み書きできるかの検証（テスト）がまだ残っています。
ユーザーさんに実際にGASをデプロイしていただき、URLを環境変数としてセットしてテストするフェーズにいます。

## 次のステップ（未着手・今後のロードマップ）
チェックリスト（`status.md`）が枯渇していても、以下のような中長期的なタスクが存在します。AIはこれを常に意識し、自律的に提案を行ってください。

*   **E2E連携テスト**: モックではなく本物のAPIを使い、インタビュー画面からのPOST保存と、管理者ダッシュボードでのデータGETが完璧に行われるかの疎通確認。
*   **例外処理とUXの向上**: GAS側でエラーが起きたり、オフラインになったりした際、ユーザーが迷わず再試行できるUIデザインのブラッシュアップ。
*   **デザイン・使い勝手のフィードバック反映**: 実際の環境で動かしてみての気づき（グラフのアニメーション、PDF出力時のレイアウトの微調整など）の改修。
*   **セキュリティ強化**: デモから実践投入へ向けて、管理画面の認証や個人情報保護の厳密化。
